participants:
  - el_type: ethrex
    el_image: ghcr.io/lambdaclass/ethrex:main
    el_extra_params:
      - "--syncmode=snap"
    cl_type: lighthouse
    cl_image: sigp/lighthouse:v7.1.0
    count: 1

network_params:
  network: hoodi

additional_services:
  - assertoor

assertoor_params:
  tests:
    - https://raw.githubusercontent.com/lambdaclass/ethrex/main/.github/config/assertoor/syncing-check.yaml
